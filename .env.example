# Copy to .env.local. Never commit real values. Cloud sessions: set these as environment variables on the cloud environment instead.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=            # server-side only
STRIPE_SECRET_KEY=                     # dealer is merchant of record via Connect; Obavia never holds funds
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
ANTHROPIC_API_KEY=
