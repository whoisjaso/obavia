/**
 * Offer Studio — owning module agent: M-script.
 * Owns: data/offers.json and this folder (alongside scripts/).
 * Planned here: offer version lifecycle, fictional-offer isolation, null-price rendering rules.
 */
export const MODULE = 'offers' as const;
